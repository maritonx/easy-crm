---
"@easy-cms/storage-s3": minor
---

New package `@easy-cms/storage-s3`: upload storage on AWS S3, Cloudflare R2, MinIO and other S3-compatible services, for hosts without a persistent disk. Files are served through the API from a private bucket by default, or from `publicUrl` (a CDN or public bucket).
