# @easy-cms/drizzle

Internal building blocks shared by the Easy CMS database adapters
([`@easy-cms/db-sqlite`](../db-sqlite), [`@easy-cms/db-postgres`](../db-postgres)):
tables built from the config, `where` → SQL, documents with child tables, and migrations.
You don't need to install it directly.
